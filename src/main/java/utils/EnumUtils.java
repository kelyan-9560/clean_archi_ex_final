package utils;

public class EnumUtils {

    /*
    public static boolean instructionsContains(String ){
        for(CommandInstructions commandInstructions : CommandInstructions.values()){
            if(commandInstructions.getInstruction().equals(input)){
                return true;
            }
        }
        return false;
    }

     */
}
